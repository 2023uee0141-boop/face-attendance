# Python dependencies for Face Recognition Attendance System
# Install with: pip install -r requirements.txt

# Core AI/ML
numpy>=1.24.0
opencv-python>=4.8.0
Pillow>=10.0.0

# Face Detection (MTCNN)
mtcnn>=0.1.1
tensorflow>=2.13.0; sys_platform != 'darwin' or platform_machine != 'arm64'
tensorflow-macos>=2.13.0; sys_platform == 'darwin' and platform_machine == 'arm64'

# Face Recognition (ArcFace via InsightFace)
insightface>=0.7.3
onnxruntime>=1.16.0

# Fast Similarity Search (HNSW)
hnswlib>=0.7.0

# Scientific Computing
scikit-learn>=1.3.0
scipy>=1.11.0
fastapi>=0.100.0
uvicorn>=0.22.0
pydantic>=2.0.0
